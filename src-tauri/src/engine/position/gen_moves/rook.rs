use crate::engine::position::game_state::GameState;

impl GameState {
    #[inline(always)]
    pub fn generate_rook_moves(&self, loc: u8) -> u64 {
        println!("brook");
        
        0x0
    }
}
