use crate::engine::position::game_state::GameState;

impl GameState {
    // legal queen moves
    #[inline(always)]
    pub fn generate_queen_moves(&self, loc: u8) -> u64 {
        println!("queen");

        0x0
    }
}
